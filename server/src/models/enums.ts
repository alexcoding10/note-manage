
export enum StatusNote {
    Active = "active",
    Inactive = "inactive",
    Replaced = "replaced",
    Deleted = "deleted"
}

export enum StatusUser {
    Active = "active",
    Inactive = "inactive",
    Deleted = "deleted"
}